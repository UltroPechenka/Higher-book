'use strict'

let array1 = [1, 2, 3];
let array2 = [a, b, c];
let array3 = [x, y, z];

let map = new Map();

map.set(array1, "Строка для первого массива");
map.set(array2, "Строка для второго массива");
map.set(array3, "Строка для третьего массива");

console.log(map);